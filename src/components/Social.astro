---
const { platform, username } = Astro.props;
---

<style>
  footer {
    display: flex;
    gap: 1rem;
    margin-top: 2rem;
  }
</style>

<a href={`https://www.${platform}.com/${username}`} style="color:var(--h6-color)">{platform}</a>