---
const { url, title, subtitle, logo } = Astro.props;
---

<a href={url} class="flex text-base p-4 rounded-2xl border border-bunker-300 dark:border-bunker-500 hover:bg-bunker-100 dark:hover:bg-bunker-900 lg:w-96 lg:min-h-36 items-center">
    <div class="flex items-center">
        <img src={logo} alt={title} class="w-20 h-20 mr-4" />
        <div>
          <p class="text-xl font-bold">{title}</p>
          <p>{subtitle}</p>
        </div>
    </div>
</a>
